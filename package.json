{
  "name": "ldstars",
  "version": "1.0.1",
  "description": "Rate (as in five ldstars linked data) a schema.org CreativeWork",
  "main": "dist/index.js",
  "scripts": {
    "test": "node_modules/.bin/mocha --compilers js:babel-register --bail",
    "build": "rm -rf dist && ./node_modules/.bin/babel src --out-dir dist",
    "prepublish": "npm run build"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/standard-analytics/ldstars.git"
  },
  "keywords": [
    "5 stars linked data"
  ],
  "author": "Sebastien Ballesteros",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/standard-analytics/ldstars/issues"
  },
  "homepage": "https://github.com/standard-analytics/ldstars",
  "dependencies": {
    "spdx-license-list": "^3.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "babel-register": "^6.24.0",
    "mocha": "^3.2.0"
  }
}
